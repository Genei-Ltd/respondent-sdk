/**
 * Zod schemas generated from the provider's OpenAPI component schemas.
 *
 * Every component schema is exported as `z<SchemaName>` — for example
 * `zGender`, `zScreenerResponseStatus`, `zProject` — so you can compose the
 * provider's enums and objects into your own schemas.
 *
 * The generated file imports from `zod/v4`, which resolves on both `zod@3.25+`
 * and `zod@4`.
 */
export * from './generated/zod.gen'
